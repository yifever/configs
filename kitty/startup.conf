layout splits

# Top-left window
launch --title=top-left

# Bottom row (3 windows)
launch --location=hsplit
launch --location=vsplit
launch --location=vsplit

# Split top-left to create top-right
focus top-left
