layout splits

# Top-left window
launch --title=top-left zsh

# Bottom row (3 windows)
launch --location=hsplit zsh
launch --location=vsplit zsh
launch --location=vsplit zsh

# Split top-left to create top-right
focus top-left
